import classNames from "classnames";
import { IList, IProps, IText } from "./mission_content.interface";

export const container = (props: IProps) => classNames("");

export const text = (props: IText) => classNames("");

export const listsContainer = (props: IList) => classNames("");

export const listContainer = (props: IList) => classNames("");

export const listText = (props: IList) => classNames("");
