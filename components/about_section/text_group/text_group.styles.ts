import classNames from "classnames";
import { IProps } from "./text_group.interface";

export const container = (props: IProps) => classNames("");

export const text = (props: IProps) => classNames("");
