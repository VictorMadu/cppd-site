import classNames from "classnames";

export const container = (props: unknown) => classNames("mt-12");

export const title = (props: unknown) => classNames();

export const textContainer = (props: unknown) => classNames();

export const authorDescContainer = (props: unknown) => classNames("mt-2");
export const authorName = (props: unknown) => classNames();

export const authorPosition = (props: unknown) => classNames();

export const authorDate = (props: unknown) => classNames();
